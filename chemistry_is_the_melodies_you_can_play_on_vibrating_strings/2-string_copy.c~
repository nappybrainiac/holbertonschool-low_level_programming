#include "my_functions.h"

char *string_copy(char *dest, const char *src){

  int i;
  int j;

  /* This loop takes the characters of src and adds them to dest */
  for(j = 0; src[j] != '\0'; j++){
    dest[i] = src[j];
    i++;
  }
  return dest;

}
