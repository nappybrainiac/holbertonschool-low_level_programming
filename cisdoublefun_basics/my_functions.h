int print_char(char c);         /* prints one character at a time used in the
                                   place of printf */
void print_alphabet(void);      /* Prints the alphabet in lowercase */
void print_tebahpla(void);      /* Prints the alphabet backwards in lowercase*/
void print_base16(void);        /* Prints the numbers that comprise base 16 */
void positive_or_not(int n);    /* Prints P for positive, N for negative and Z
                                   for zero values*/
char odd(int n);                /* Prints 0 for odd and E for even numbers*/
